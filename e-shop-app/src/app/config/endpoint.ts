const RESOURCE = {
  products: 'products',
  categories: 'categories'
}

export const ENDPOINT = {
  products: {
    index: `${RESOURCE.products}`
  },
  categories: {
    index: `${RESOURCE.categories}`
  }
}
